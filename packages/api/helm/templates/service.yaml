apiVersion: v1
kind: Service
metadata:
  name: {{ include "bildschirm.fullname" . }}
  labels:
    {{- include "bildschirm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bildschirm.selectorLabels" . | nindent 4 }}
